console.log('Sim sim salabim');

// divide x by y and check for remainder = 0
var numAr = [0],
    finalNum = 0,
    checkNum = 0

var divNum = function(y) {
    checkNum = finalNum / y;
    return checkNum;
};

for (var x = 0; x <= 20; x++) {
    var y = 1;

    if (y % x == 0) {

    }
}
